{
  "description": "Preserve frozen route identity for Kubernetes Job generation.",
  "id": "adapter.orchestrator.kubernetes",
  "integration_model_version": "0.1",
  "limitations": [
    "The export is a portable manifest, not deployable native code; an authorized adapter must enforce runtime, secret, retry, and isolation policy."
  ],
  "output_kind": "projection.frozen-plan",
  "source_kind": "orchestrator.kubernetes-job",
  "supported_versions": [
    "batch/v1"
  ]
}
